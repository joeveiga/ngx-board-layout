import { BoardCardDirective } from './board-card.directive';

describe('BoardCardDirective', () => {
  it('should create an instance', () => {
    const directive = new BoardCardDirective();
    expect(directive).toBeTruthy();
  });
});
